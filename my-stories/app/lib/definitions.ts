//type definitions for data

export type User = {
    id: string,
    name: string,
    email: string, 
    password: string
};

export type Books = {
    id: string,
    title: string,
    author: string,
    cover_image: string
}